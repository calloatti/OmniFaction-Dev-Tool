[file name]: AGENTS.md
[file content begin]
Include ..\AGENTS.md

# OmniFaction Dev Tool — Mod-Specific Agent Instructions

## Identity
- **Mod ID:** `Calloatti.OmniFactionDevTool`
- **Assembly:** `OmniFactionDevTool`
- **Namespace:** `Calloatti.OmniFactionDevTool`
- **Framework:** Harmony patches + one Bindito DI singleton (`OmniFactionService`) — no UI, no config
- **Min Game Version:** 1.0.12.5 (forks: `Version-1.0` targets 1.0.x, `Version-1.1` targets 1.1.x — see below)
- **Entry Point:** `ModStarter.cs` — `IModStarter`, `Harmony.PatchAll()`, Harmony ID `"com.mod.unlockallfactionbuildings"`

## What This Mod Does
Developer tool (not for normal play) that unlocks buildings, goods, materials, and needs across **all factions** on a single map for faction-testing. It does this entirely through Harmony patches that make several game services tolerant of the multi‑faction/multi‑spec layouts the dev map it ships with relies on.

## In-Progress Feature: Per-Faction Population Counter
The full design lives in **`population-counter.md`** (repo root). It records the lifecycle findings (singleton `PostLoad()` is *before* map load; `ShowPrimaryUIEvent` is the game's "everything loaded" signal), the event-driven design, and the verified API facts. **Status: implemented in both `Version-1.1/` and `Version-1.0/`** (the counter service + `PopulationCounterPatches.cs`, plus the `DoNotPublicize` event items). 1.1 is runtime-verified; the 1.0 back‑port per `Version-1.0/AGENTS.md` is built green (0 warnings / 0 errors). Note: in `Version-1.0/` the counter service was renamed from `FactionPopulationService` to `OmniFactionService` when it gained the DC‑faction starting‑population spawn (see its source‑table row); `Version-1.1/` still uses the old name until ported.

## Performance Optimisations (As of 2026‑08‑08)
Several hot‑path methods have been optimised to reduce CPU usage, especially in large colonies:

- **Entity Faction Cache** (`OmniFactionService.SetFactionForEntity` / `GetCachedFaction` / `RemoveFactionForEntity`): stores each entity's faction ID keyed by `GameObject.GetInstanceID()` when the entity is created or registered. `FactionAssignmentHelper.GetFactionID` checks this cache first, avoiding expensive string scans on every call.
- **Pre‑filtered Need Lists** (`FactionNeedCache.FactionBeaverNeedSpecs` and `FactionBotNeedSpecs`): `NeedVerifier.Load` now builds separate `List<NeedSpec>` per faction for beavers and bots directly from `ISpecService.GetSpecs<NeedSpec>()`. `NeedSpec.CharacterType` buckets each spec exactly (`"Beaver"`/`"Bot"`; specs with neither are in no list, mirroring vanilla `FactionNeedService.GetBeaverNeeds`/`GetBotNeeds`), and each list is sorted by `NeedSpec.Order` with a loader‑index tie‑break (stable — preserves the vanilla need order the UI displays, instead of the alphabetical `GetSpecs<NeedSpec>()` loader order). `NeedManager.GetNeeds` returns the appropriate list based on the entity's component type (`Beaver` or `Bot`), eliminating LINQ `Where` and repeated scans.
- **Wellbeing Counters** (`WellbeingPatches.cs`): `Patch_WellbeingService_AppliedNeeds` uses a static `Dictionary` that is cleared and refilled each update, avoiding per‑update allocations. The UI patch reads from this static dictionary.
- **Workplace Assignment** (`WorkplaceAssigner.Assign`): uses a `HashSet<Worker>` for O(1) removal of assigned unemployed workers, instead of copying a list and performing O(n) `Remove`.
- **Attachment ID Rewrite** (`TemplateAttachments.GetOrCreateAttachment`): caches rewritten attachment IDs per `(originalId, entityFaction)` and uses a `HashSet<string>` per spec for O(1) existence checks, reducing repeated string replaces and list scans.
- **UI Suffix** (`LabeledEntity.DisplayName`): no longer performs fallback substring scans; only appends the suffix when a cached `FactionSpec.DisplayNameLocKey` exists, eliminating UI‑update‑frequency string searches.

These changes keep the mod performant even with many entities and frequent UI updates.

## Source Architecture (`Version-1.0/Source/`)

> The `Version-1.1/` fork shares this exact source layout — the patches are identical, only the API‑drift fixes differ. See the "Version-1.1 fork (game 1.1)" section below and read `Version-1.1/AGENTS.md` for the complete 1.0→1.1 port notes before touching `Version-1.1/Source/`.

| File | Role |
|---|---|
| `ModStarter.cs` | `IModStarter` entry point — creates a Harmony instance and `PatchAll()`. |
| `GameFactionSystemPatches.cs` | Five patches on `Faction*CollectionIdsProvider` (`Template`, `Good`, `Material`, `Need`) that **aggregate** collection IDs across all `FactionSpec`s in `FactionService` instead of only the current faction; plus a `FactionNeedService.GetBeaverOrBotNeedById` prefix that safely falls back across beaver/bot need lists. |
| `TemplateCollectionSystemPatches.cs` | `TemplateCollectionService.Load` postfix that **deduplicates** blueprints by both object reference and `TemplateSpec.TemplateName`, keeping the first loaded copy, AND builds `FactionBlueprintCache` (TemplateName → faction identity from `FactionSpec.Id`, plus a separate `TemplateToFactionLocKey` display map) from faction/collection specs. `LabeledEntity.DisplayName` postfix appends a faction suffix to display names **only when a locKey is cached** — no fallback scans. |
| `TemplateSystemPatches.cs` | `TemplateNameMapper.TryAddTemplate` prefix that forces `throwIfDuplicated = false` to bypass duplicate‑name exceptions. |
| `BeaversPatches.cs` | `BeaverFactory.Load` prefix — collects **every** `AdultSpec`/`ChildSpec` blueprint into `Patch_BeaverFactory_Load.AllAdultTemplates`/`AllChildTemplates` and caches each. `BeaverFactory.Create*` prefixes inject a factioned template into `_adultTemplate`/`_childTemplate` before the original runs, so `BeaverAdult.Folktails` / `BeaverAdult.IronTeeth` beavers coexist. Faction source per spawn site: `CreateAdult` (**DC‑spawn faction** via `StartingBeaverSpawn.PendingFaction` when set by `OmniFactionService`, else **starting faction** via `PickAdultTemplateForStartupFaction` — `FactionService.Current` while the game is still starting up, so the game's own starting‑settlement spawn gets the player's faction — else **nearest District Center's faction** via `OmniFactionService.FindNearestDistrictFaction(position)`, else **round‑robin** — dev tool), `CreateChild` (**spawning building's faction** via `Patch_NewbornSpawner.PendingFaction`, then **DC‑spawn faction** via `StartingBeaverSpawn.PendingFaction`, then **starting faction** during startup, then **nearest District Center's faction**, fallback round‑robin — covers `CreateNewbornChild`, BreedingPods/ProcreationHouses/dwelling‑UI spawns, and modded buildings resolving via `GetFactionID`), `CreateNewbornAdult` (**spawning building's faction**, fallback round‑robin — BreedingPods), `CreateAdultFromChild` (**the child's own faction**, fallback round‑robin — `Child.GrowUp`). `Patch_NewbornSpawner` patches both `NewbornSpawner.SpawnAdult`/`SpawnChild` (prefix sets `PendingFaction` from `FactionAssignmentHelper.GetFactionID(spawner)`, `"Common"` → null, postfix clears). `StartingBeaverSpawn` is a static `PendingFaction` set only around `OmniFactionService`'s DC starting‑population spawn calls. `BeaverTextureSetter.Start` prefix — **applies the entity's own faction fur texture** (first element of that faction's `Textures`/`ChildTextures`, matched from the GameObject name), falling back to round‑robin for unsuffixed names; the 1‑5 texture variants are role‑applied later by the game. The faction‑name match + fur‑apply logic lives in the shared `FactionFurHelper` (`FindFactionForName`/`TryApplyFactionFur`). |
| `BotsPatches.cs` | `BotFactory.Load` prefix — collects **every** `BotSpec.Blueprint`, caches each, and stores them in `Patch_BotFactory_Load.AllBotTemplates`. `Manufactory.IncreaseProductionProgress` prefix/postfix tracks `ActiveManufactory`. `BotFactory.Create(Vector3, Quaternion)` prefix — **spawns the bot matching the producing building's faction** (via `ActiveManufactory` + `FactionAssignmentHelper`), else the **DC‑spawn faction** (`StartingBeaverSpawn.PendingFaction`), else the **nearest District Center's faction** (`OmniFactionService.FindNearestDistrictFaction(position)` — dev‑tool spawns), falling back to **round‑robin** through `AllBotTemplates` (works for any number of factions). |
| `NeedManagerPatches.cs` | `FactionNeedCache.FactionAllowedNeeds` (FactionId → set of Need Ids) and the two pre‑computed lists `FactionBeaverNeedSpecs` and `FactionBotNeedSpecs` (FactionId → `List<NeedSpec>`) built in a `NeedVerifier.Load` postfix. Each faction's allowed need ids are the union of its `NeedCollectionIds` and the `"Common"` collection. Then `NeedSpec.CharacterType` buckets each `NeedSpec` exactly (`"Beaver"`/`"Bot"`; specs with neither are in no list, mirroring vanilla `FactionNeedService.GetBeaverNeeds`/`GetBotNeeds`), and each list is sorted by `NeedSpec.Order` with a loader‑index tie‑break (stable — preserves the vanilla need order the UI displays, instead of the alphabetical `GetSpecs<NeedSpec>()` loader order). `NeedManager.GetNeeds` postfix first determines the entity's faction (via the entity cache or a single fallback scan), then checks whether the entity has a `Beaver` or `Bot` component, and returns the corresponding pre‑computed `List<NeedSpec>` — ensuring beavers never receive bot needs (like `Energy`) and bots never receive beaver needs (like `Fun`), in addition to faction‑specific filtering. Unsuffixed names fall back to a single substring scan only once before cache is populated. |
| `WellbeingPatches.cs` | The population wellbeing panel divides every need by the **total** beaver count, so faction‑specific needs can never reach 100% and max wellbeing looks unreachable. `WellbeingService.AppliedNeeds` postfix rebuilds a static `Dictionary<string, int> EligibleNeedCounts` (need id → number of beavers that actually have that need, requiring `WellbeingTrackerRegistrar`, scope global/district matches automatically). `PopulationWellbeingBox.UpdateCounters` prefix reimplements the counter update with the per‑need eligible count as the denominator (a need nobody carries shows "0 / 0"). The dictionary is reused to avoid per‑update allocations. Requires `Timberborn.Wellbeing` + `Timberborn.WellbeingUI` publicized. |
| `WorkSystemPatches.cs` | `FactionAssignmentHelper.GetFactionID` — first checks the entity faction cache (`OmniFactionService.GetCachedFaction`), then `FactionBlueprintCache.TemplateToFactionId` (TemplateName → `FactionSpec.Id`), then a single fallback substring match for uncached entities (e.g., during creation). `CanWorkAt` and `CanResideAt` provide the "Common" wildcard; `GetWorkplaceFaction` returns `"Common"` for `DistrictCrossing` (a crossing links two districts and must be staffable by any faction regardless of its own factioned template). Single `WorkplaceAssigner.Assign` prefix (returns `false` — fully reimplements `Assign`) **restricts worker assignment to the worker's own faction — applies to BOTH bots and beavers** (beavers were formerly exempt; the per‑faction population counter relies on each faction's workforce staying in its own buildings). Iterates **every** understaffed workplace in priority order (`_priorityOrderedWorkplaces._workplaces.Values`, a `SortedList` ordered highest‑priority‑first — matching vanilla's `ReassignWorkersToHigherPriorityWorkplaces` cadence; paused workplaces are skipped automatically because `Understaffed` is false when the `BlockableObject` is blocked, decompiled `Timberborn.WorkSystem.cs` L1473–1483); per workplace first fills from compatible unemployed workers using a `HashSet<Worker>` for O(1) removal, then falls back to migrating compatible workers from the lowest‑priority staffed workplace with a strictly lower `Priority`. The migration pass runs even with **zero** unemployed workers (the fill loop no‑ops, then migration still runs), and there is no single‑workplace early return. |
| `DwellingSystemPatches.cs` | `AutoAssignableDwelling.CanAssignDweller` prefix — **restricts home assignment to the beaver's own faction** via `FactionAssignmentHelper.CanResideAt` (same "Common" wildcard rule as `CanWorkAt`). Dwellings have no priority ordering, so gating the single `CanAssignDweller` decision point is sufficient — `DwellerHomeAssigner`'s stalest‑dwelling loop just skips rejected dwellings. **Not** gated: `Dweller.AssignToDwellingAfterLoad` (save restore — re‑establishes the saved home without evicting pre‑restriction beavers) and `CharacterBirth.PreInitializeEntity` (newborns are assigned to the spawning building's own dwelling, and `Patch_NewbornSpawner` already makes the newborn that building's faction, so that path is inherently same‑faction). |
| `OmniFactionService.cs` | **Bindito DI singleton (`[Context("Game")]` configurator)** — the per‑faction population counter's data source AND the per‑faction starting‑population spawn. Registers with the `EventBus` in `ILoadableSingleton.Load()` (early posts are queued and delivered at `EventBus.PostLoad()`, by which point entity `Load()` has restored districts/needs/contamination — no `ShowPrimaryUIEvent` re‑seed needed). Exposes statics `StartupComplete` (true from `ShowPrimaryUIEvent`) and `CurrentFaction` (`FactionService.Current.Id` — the player's starting faction) so the `BeaverFactory.Create*` prefixes route the game's own starting‑settlement spawn to the right faction. Tracks global + per‑district tallies keyed by `FactionAssignmentHelper.GetFactionID`: characters (beaver adult/child/bot, workforce employable/unemployable via `WorkRefuser`, contamination via `Contaminable`), dwellings (mirrors vanilla `DwellerCounter`), workplaces (mirrors vanilla `WorkplaceWorkerCounter`, beaver/bot split via `WorkplaceWorkerType.WorkerType`, `null` → Beaver). Subscribes the *unpublicized‑assembly* events directly (`Contaminable.ContaminationChanged`, `BlockableObject.ObjectBlocked/ObjectUnblocked`); the *publicized‑assembly* events it needs (`Citizen.ChangedAssignedDistrict`, `WorkRefuser.RefusesWorkChanged`, `Dwelling.NumberOfDwellersChanged`, `Workplace.WorkerAssigned/WorkerUnassigned/DesiredWorkersChanged`, `WorkplaceWorkerType.WorkerTypeChanged`, `DistrictBuilding.ReassignedDistrict`) stay subscribable thanks to the `DoNotPublicize` items in the csproj (see below). **Entity faction cache** — static methods `SetFactionForEntity`, `GetCachedFaction`, `RemoveFactionForEntity` store/retrieve faction ID by `GameObject.GetInstanceID()`. **`FindNearestDistrictFaction(position)`** — static helper scanning `DistrictCenterRegistry.FinishedDistrictCenters` for the nearest DC by distance and returning its faction (`null` when none, or when the nearest DC is `"Common"`); the `BeaverFactory.Create*`/`BotFactory.Create` prefixes use it so dev‑tool and unfactioned‑spawner spawns become the nearest DC's faction. `FillPopulationData` folds a faction tally into `PopulationData` with the vanilla derived formulas (`Homeless = adults + children − occupied beds`, `Unemployed = max(employable − occupied workslots, 0)`). **Starting‑population spawn:** on `EnteredFinishedStateEvent` for a `DistrictCenter` in a live game, replicates the new‑game start for that DC's faction (`StartingBeaversInitializer` + `StartingGoodsProvider`): `GameModeSpec.StartingAdults`/`StartingChildren` at the DC's `UnblockedSingleAccess` with the mode's age‑progress spread (via `BeaverFactory.CreateAdult/CreateChild` with `StartingBeaverSpawn.PendingFaction` set), plus `StartingFood` Berries / `StartingWater` Water into the DC's `SimpleOutputInventory` (`GiveIgnoringCapacity`). Gates: skipped before `ShowPrimaryUIEvent` (covers the starting settlement DC on new game AND every DC restored on save load — restored buildings re‑post `EnteredFinishedStateEvent` at `EventBus.PostLoad()`, which runs before `ShowPrimaryUIEvent`, so existing DCs never respawn on reload), skipped for `"Common"`, and skipped while the faction already has any beavers (the starting faction is untouched). Game mode read via `ISceneLoader.GetSceneParameters<GameSceneParameters>()` when `NewGame`, else `GameModeSpecService.GetDefaultSpec()` (mirrors the game's own fallback). |
| `PopulationCounterPatches.cs` | `PopulationCounter.Sample` postfix — swaps `_sampledPopulationData` for the counter's own faction's data (`GetGlobal(faction)` or `GetDistrict(__instance._districtBuilding.GetInstantOrConstructionDistrict(), faction)`), then re‑runs `UpdateOutputState()`. Skips `"Common"` (non‑factioned) counters so vanilla behavior is untouched. All 18 modes work because `GetMeasurement()` reads every value from the swapped `PopulationData`. |
| `TemplateAttachmentPatches.cs` | `TemplateAttachments.GetOrCreateAttachment` prefix — rewrites other‑faction names in attachment IDs to the entity's faction, returns cached attachments, and **creates a dummy inactive attachment instead of crashing** when the id isn't in the spec. Uses a cache `(originalId, entityFaction) → rewrittenId` and a `HashSet` per spec for O(1) existence checks to avoid repeated scanning. |
| `EntityAvatarPatches.cs` | `BeaverEntityBadge.GetEntityAvatar` + `BotEntityBadge.GetEntityAvatar` postfixes — reroute the selected character's portrait from `FactionService.Current`'s avatar to the entity's OWN faction's avatar (via `EntityFactionAvatarHelper.ResolveFaction`, matched from `GameObject.name` exactly like `Patch_BeaverTextureSetter_Start`), preserving the contaminated/child/bot variants. Unsuffixed/shared entities match no faction and keep the vanilla current‑faction fallback. |
| `EmptySlotAvatarPatches.cs` | A building's **vacant** character slots (dwelling lodges, workplace rows) render `FactionService.Current`'s avatar as the disabled placeholder, so every empty slot showed the current faction's face. `Patch_WorkerView_ShowEmpty` (workplace faction from `_workplaceWorkerType`), `Patch_DwellingUserFragment_ShowFragment`/`UpdateFragment` (dwelling faction from `_dwelling`/the `entity` arg) push the building's faction into `EntityFactionAvatarHelper.PendingEmptySlotFaction` (prefix set / postfix clear); `CharacterButton.ShowAdultEmpty`/`ShowChildEmpty`/`ShowBotEmpty` postfixes then show that building's `Avatar`/`ChildAvatar`/`BotAvatar`. Unfactioned/"Common" buildings and renders outside a building panel keep vanilla behavior. Requires `Timberborn.CharactersUI`, `Timberborn.DwellingSystemUI`, `Timberborn.WorkSystemUI` publicized (added to `CommonModSettings.props`). |
| `WorkerOutfitPatches.cs` | `WorkerOutfitService.Load` postfix — the game's outfit dictionary is built from **only** the current faction's `WorkerOutfitSpec`s, so non‑current‑faction beavers/bots can't resolve their workplace's outfit. Postfix unions every other faction's specs into `_workerOutfitSpecs` (outfit ids embed the faction, so `GetSpecKey` never collides). Requires `Timberborn.WorkerOutfitSystem` publicized (added to `CommonModSettings.props`). |
| `ModularShaftsPatches.cs` | `ShaftFrameFactory.Load` / `ShaftModelFactory.Load` prefixes — re‑create the shaft root and part GameObjects from the first `ModularShaftPartsSpec` instead of relying on `GetSingle<T>()`. |
| `GetSingleFactoryPatches.cs` | Three more `GetSingle<T>` crash‑sites hardened the same way: `BlockOccupationLayerFactory.Load` (`BlockOccupierSpec`), `RecoveredGoodStackFactory.Load` (`RecoveredGoodStackSpec`, incl. its `Blocks.Single()`), and `PlaneSpawner.Awake` (`PlaneSpec`, re‑reads `SpawnPointName` + `FindChildTransform`). |
| `PlantingUIPatches.cs` | `PlantingToolButtonFactory.GetPlanterBuildingName` prefix — uses `FirstOrDefault()` on `PlanterBuildingSpec` and resolves the display name via `ILoc`. |

## Content Blueprints (`Characters/` and `TemplateCollections/` folders at mod root)

The mod ships **six factioned beaver blueprints** (Folktails/IronTeeth/Emberpelts × Adult/Child) plus three **template‑collection files** to give new beavers faction‑specific needs and fur. These live at the mod root (`Version-1.0/Characters/Beaver/...`, `Version-1.0/TemplateCollections/...`) — NOT under a `Blueprints/` folder. `Version-1.1/` ships its own copies regenerated from the 1.1 vanilla blueprint (see "Version‑1.1 fork" below).

**Modded factions (e.g. Emberpelts) get zero beavers unless they ship factioned beaver blueprints themselves** — see `modded-faction-beavers.md` for the full pattern, the standalone‑spec fix, and the `.optional`/load‑order trap to avoid. Summary below.

- `Characters/Beaver/Beaver{Adult,Child}.{Folktails,IronTeeth,Emberpelts}.blueprint.json` — exact copies of the vanilla `BeaverAdult.blueprint.json`/`BeaverChild.blueprint.json` with **only** `TemplateSpec.TemplateName` suffixed (e.g. `BeaverAdult.Folktails`). The spawned `GameObject.name` then embeds the faction, which is what the `NeedManager.GetNeeds` filter and the `BeaverTextureSetter` faction‑match rely on. `WellbeingTierService` matches tiers by component, not template name, so the renames are safe.
- **Regenerating (1.0):** copy from `timberborn-decompiled-1.0.13.1-b769e88-sw\Blueprints\Characters\Beaver\Beaver{Adult,Child}.blueprint.json` and replace `"TemplateName": "Beaver{Adult,Child}",` → `"TemplateName": "Beaver{Adult,Child}.<Faction>",`. Nothing else in the file changes — both factions' hats/attachments and the shared model are already in the vanilla template. **Regenerating (1.1):** same procedure but from `timberborn-decompiled-1.1.1.1-cfb778f-xsw\...` (the 1.1 vanilla blueprint also carries new `ScavengerHatBeaver.*`/`ForesterHatBeaver.*` attachments, `CharacterBirthSpec` instead of `CharacterBirthNotifierSpec`, a `DeadStatusSpec` split into three loc keys, and extra `Jumping`/`Punching`/`Stepping` animator states — the factioned copies must inherit those too).
- `TemplateCollections/TemplateCollection.Characters.{Folktails,IronTeeth}.blueprint.json` — overrides that use **`Blueprints#append`** to add the factioned beavers to the per‑faction character collection. **Do not overwrite the `Blueprints` array wholesale** — the shared `BeaverAdult.blueprint`/`BeaverChild.blueprint` must stay loadable because **existing saves reference those template names**; replacing the list deletes all beavers on save load. The `#append` keyword (`Timberborn.SerializationSystem.cs` `JsonKeywords`) merges the array items into the vanilla list. (Base‑game factions only — `#append` is load‑order‑fragile across mods, see `modded-faction-beavers.md`.)
- `TemplateCollections/TemplateCollection.Characters.Emberpelts.blueprint.json` — **standalone** `TemplateCollectionSpec` with `CollectionId: "Characters.Emberpelts"` + a full `Blueprints` array (no `#append`, no `.optional`). `TemplateCollectionService.Load` unions blueprints across all specs sharing a `CollectionId`, so this merges with the Emberpelts mod's own collection **regardless of load order**, and is a harmless orphan when the mod is absent.
- Because `PickTemplate` filters the round‑robin pool to `IsFactioned` templates, new beavers always spawn from the factioned copies; the shared templates only exist for save compatibility.
- Deploying the folders is handled outside the build (copy `Characters/` and `TemplateCollections/` into the deployed mod folder); the pre/post build scripts copy the compiled `bin` output only.

## Core Principle
Every patch exists to survive the **duplicated specs / templates** that appear when all factions are loaded into one map. The vanilla code calls `GetSingle<T>()` or throws on duplicate template names; this mod's patches replace those failures with safe `FirstOrDefault()` dedup/handling and **fall back to the original method** (return `true`) whenever the expected data is missing. Do not break that fallback contract.

## Known Pitfalls & Lessons Learned
- **No reflection — rely on publicized assemblies.** All game fields/methods/private setters this mod touches live in assemblies publicized via Krafs.Publicizer (see `Publicize` includes in the mod `.csproj` and in `CommonModSettings.props`), so patches access `_factionSpecService`, `_adultTemplate`, `_botTemplate`, `ShaftFrameFactory.Instantiate`, `TemplateCollectionService.AllTemplates` (private set), `FactionSpec.DisplayNameLocKey`, etc. **directly** — no `AccessTools`/`Traverse`/`TargetMethod` reflection. If the game renames a field, the build fails loudly instead of the patch silently no‑oping at runtime.
- **Publicizer `Unsafe` strategy turns field‑like events' compiler‑generated backing fields into public fields that collide with the event name** (CS0229 on `+=`). Publicizer's documented fix is `<DoNotPublicize>` items for just those backing fields — the mod's `.csproj` lists the eight events `OmniFactionService` subscribes to (`Citizen.ChangedAssignedDistrict`, `DistrictBuilding.ReassignedDistrict`, `Dwelling.NumberOfDwellersChanged`, `WorkRefuser.RefusesWorkChanged`, `Workplace.WorkerAssigned`/`WorkerUnassigned`/`DesiredWorkersChanged`, `WorkplaceWorkerType.WorkerTypeChanged`). The events in *unpublicized* assemblies (`Contaminable.ContaminationChanged`, `BlockableObject.ObjectBlocked`/`ObjectUnblocked`) need no entry. Keep these lists in sync if the service subscribes to more events.
- `Timberborn.FactionSystem` is publicized specifically to expose `FactionSpec.DisplayNameLocKey` (a `private` property) for the `FactionBlueprintCache` build — do not remove it from the `.csproj`.
- The four `Faction*CollectionIdsProvider` postfixes all funnel through one shared helper `FactionCollectionIdsAggregator.CombineWithAllFactions` with a per‑provider `Func<FactionSpec, ImmutableArray<string>>` selector.
- **Prefix patches return `bool`** — `false` skips the original, `true` falls back. After assigning template fields (directly, thanks to publicizer), always call `templateInstantiator.CacheInstance(...)`.
- **Dedup keeps the first loaded version** of a template name (`TemplateCollectionSystemPatches.cs`) — the intended behavior is "first wins". Because the collection‑provider aggregation seeds the **current faction's** IDs first, the current faction's templates win duplicate‑name clashes — do not reorder `FactionCollectionIdsAggregator` or that preference silently flips.
- **Faction matching is by string substrings** (`IndexOf("Folktails"/"IronTeeth")`). Bot/beaver template names embed the faction (`Bot.Folktails`), `GameObject.name` carries it at `Awake` time (instantiation sets `name = blueprint.Name` before `NeedManager.Awake`), and buildings' `TemplateSpec.TemplateName` resolves through `FactionBlueprintCache`. `FactionNeedCache`/`FactionBlueprintCache` are populated in `ILoadableSingleton.Load` postfixes (`NeedVerifier`, `TemplateCollectionService`), so they are ready before any entity spawns. The entity faction cache (`OmniFactionService.SetFactionForEntity`) is also populated on entity creation, providing O(1) lookups thereafter.
- **Mod load order is NOT hard‑enforced by `OptionalMods`.** `ModSorter.SortByDependencies` uses `RequiredMods.Concat(OptionalMods)` (Modding.cs:757) but `Sort` re‑applies `ModPlayerPrefsHelper.GetModPriority` offsets (Modding.cs:743), so saved in‑game load order overrides the dependency sort. Therefore cross‑mod `Blueprints#append` merges are order‑fragile (plain arrays merge with `MergeArrayHandling.Replace`). For modded factions use a **standalone** `TemplateCollectionSpec` sharing the `CollectionId` instead — `TemplateCollectionService.Load` unions blueprints across all matching specs regardless of load order. Full detail in `modded-faction-beavers.md`.
- **Localization:** not used — this is a dev tool with no user‑facing UI; do not add localization unless explicitly requested.

## Version‑1.1 fork (game 1.1)

- `Version-1.1/` is a parallel fork of `Version-1.0/` targeting game 1.1.x (Min Game Version `1.1.0.0`). The source is byte‑for‑byte identical to `Version-1.0/Source/` except for the API‑drift fixes below. **Do not re‑sync `Version-1.1/Source/` wholesale from `Version-1.0/`** or these fixes are lost.
- Builds against the **experimental** game DLLs via `<TimberbornPath>C:\Program Files (x86)\Steam\steamapps\common\timberborn_experimental\Timberborn_Data\Managed</TimberbornPath>` in `Version-1.1/OmniFaction Dev Tool.csproj` (a copy of `CommonModSettings.props` + the `Version-1.0` csproj, with that path override). Reference decompiled source for 1.1: `timberborn-decompiled-1.1.1.1-cfb778f-xsw`.
- **API drift 1.0.13.1 → 1.1.1.1 (all four fixes are in `Version-1.1/Source/`):**
  1. `BeaverTextureSetter.Start` → `BeaverTextureSetter.InitializeEntity` (`IStartableComponent.Start` replaced by `IInitializableEntity.InitializeEntity`).
  2. `BotFactory.Create(Vector3, Quaternion)` → `Create(Vector3, Quaternion, object initComponent)` — patch target args must include `typeof(object)`.
  3. `RecoveredGoodStackFactory._recoveredGoodStackTemplate` field type `BlockObjectSpec` → `Blueprint` — the prefix now assigns the spec's `Blueprint` and resolves `GoodStackBlockSpec` via `GetSpec<BlockObjectSpec>().Blocks.FirstOrDefault()`.
  4. `BeaverFactory.CreateChild` gained a private overload `CreateChild(EntitySetup.Builder, Vector3, float)` — the patch target is disambiguated with `{ typeof(Vector3), typeof(float) }`.
- **Everything else verified unchanged** (no edit needed): `BeaverFactory.Load/CreateAdult/CreateNewbornAdult/CreateAdultFromChild`; `NewbornSpawner.SpawnAdult/SpawnChild`; `Manufactory.IncreaseProductionProgress` + `BotManufactory.OnProductionFinished` (still raises `ProductionFinished` inside `IncreaseProductionProgress`); the four `Faction*CollectionIdsProvider` getters; `FactionNeedService`; `NeedVerifier.Load`; `NeedManager.GetNeeds` (still called from `InitializeNeeds`); `TemplateNameMapper.TryAddTemplate`; `TemplateService.GetAll/GetSingle`; `TemplateCollectionService.Load`; `TemplateAttachments.GetOrCreateAttachment`; `BlockOccupationLayerFactory`; `PlaneSpawner`; `ShaftFrameFactory.Load`/`ShaftModelFactory.Load`; `LabeledEntity.DisplayName`; `WellbeingService.AppliedNeeds`; `PopulationWellbeingBox.UpdateCounters`/`PopulationWellbeingCounter.UpdateValues`; `WorkplaceAssigner.Assign`; `PlantingToolButtonFactory.GetPlanterBuildingName`; `IModStarter`/`IModEnvironment`; `CharacterMaterialModifier.SetTexture`.
- **Content:** vanilla 1.1 still ships **shared** `BeaverAdult`/`BeaverChild` templates (only bots are factioned), so the mod's factioned beaver copies remain required. The 1.1 vanilla beaver blueprint added `ScavengerHatBeaver.*`/`ForesterHatBeaver.*` attachments, `CharacterBirthSpec` (replacing `CharacterBirthNotifierSpec`), a split `DeadStatusSpec`, and new animator states — `Version-1.1/Characters/Beaver/*.blueprint.json` were regenerated from it (only `TemplateName` suffixed, verified via diff). `TemplateCollections/` and the `NeedCollection`/`FactionSpec` content (`Common` + per‑faction need collections, `Energy` in IronTeeth) are unchanged from 1.0.
- Full per‑file port notes, verification diffs, and the porting checklist live in `Version-1.1/AGENTS.md`.

## Build & Deploy
- Build via `dotnet build` in `Version-1.0/` (project `OmniFaction Dev Tool.csproj`). The `Version-1.1` fork builds the same way from `Version-1.1/`.
- Pre/post build scripts (`prebuild.ps1`/`postbuild.ps1`) handle assembly copying.
- `CommonModSettings.props` defines Timberborn game DLL references and publicizer configuration. The mod's own `.csproj` adds `Publicize` includes for `Timberborn.GameFactionSystem`, `Timberborn.FactionSystem`, `Timberborn.Beavers`, `Timberborn.Bots`, `Timberborn.BlockObstacles`, `Timberborn.RecoveredGoodSystem`, `Timberborn.WonderPlanes`, `Timberborn.ModularShafts`, `Timberborn.TemplateCollectionSystem`, `Timberborn.TemplateAttachmentSystem`, `Timberborn.WorkSystem`, `Timberborn.Wellbeing`, `Timberborn.WellbeingUI`, plus the `DoNotPublicize` items for the publicized‑assembly events `OmniFactionService` subscribes to (see Known Pitfalls).
[file content end]